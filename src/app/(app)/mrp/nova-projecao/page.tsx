import { MrpPlaceholder } from "../_placeholder";
export default function Page() {
  return (
    <MrpPlaceholder
      titulo="Nova projeção"
      descricao="Wizard de 3 passos: demanda → cálculo → gerar solicitação."
      etapa={6}
    />
  );
}
