import { MrpPlaceholder } from "../_placeholder";
export default function Page() {
  return (
    <MrpPlaceholder
      titulo="Histórico de projeções"
      descricao="Lista de todas as projeções calculadas + comparativo planejado × comprado."
      etapa={8}
    />
  );
}
